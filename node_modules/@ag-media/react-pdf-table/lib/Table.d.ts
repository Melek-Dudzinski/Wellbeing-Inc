import { View } from '@react-pdf/renderer';
import { Style } from '@react-pdf/types';
import React from 'react';
export interface TableProps extends Omit<React.ComponentProps<typeof View>, 'children'> {
    weightings?: number[];
    tdStyle?: Style | Style[];
    trStyle?: Style | Style[];
    children: React.ReactElement | React.ReactElement[] | React.ReactFragment | React.ReactFragment[];
}
export interface TableContextType {
    weightings?: number[];
    style: Omit<Style, 'borderWidth'> & {
        borderWidth: number;
    };
    tdStyle?: TableProps['tdStyle'];
    trStyle?: TableProps['trStyle'];
}
export declare const tableContext: React.Context<TableContextType>;
export default function Table({ children, weightings, style: styleProps, tdStyle, trStyle, ...rest }: TableProps): React.JSX.Element;
//# sourceMappingURL=Table.d.ts.map